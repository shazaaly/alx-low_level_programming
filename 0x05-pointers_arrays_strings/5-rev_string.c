#include "main.h"
/**
* reverse_string - Reverses a string
*
* @str: The string to reverse
*/
void rev_string(char *s)
{
	int i = 0;

    while (s[i])
    {
        i++;
    }

    while (i--)
    {
	char rev = '';
        rev = s[i];
    }
}
